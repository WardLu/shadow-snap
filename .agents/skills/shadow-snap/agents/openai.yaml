interface:
  display_name: "Shadow Snap"
  short_description: "Repo-specific patterns and workflows for shadow-snap"
  default_prompt: "Use the shadow-snap repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true